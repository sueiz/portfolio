$(function () {
    for(var i=0; i<3; i++){
    $('.st0').clone().appendTo('svg')}
    

    // const video = document.querySelector("video");
    // if (window.matchMedia('(prefers-reduced-motion)').matches) {
    // video.removeAttribute("autoplay"); 
    // video.pause();
    // }
});